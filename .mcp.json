{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--caps", "core,vision", "--viewport-size", "1280x720", "--browser", "chromium", "--no-sandbox"]
    }
  }
}
