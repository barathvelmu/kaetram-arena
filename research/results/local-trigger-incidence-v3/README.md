# Trigger-incidence V3 result

## Status

Complete. The different-panel V3 extension executed all 1,200 registered
requests with zero failures. The producer analysis and two independently
implemented audits agree on every cell, contrast, seed-heterogeneity count,
and the inherited directional verdict.

V3 changes only the retained state panel. It uses 20 fourth-decision states
from a separate retained round-3 evaluation rollout, with zero V2 source-path
overlap and zero rendered-message SHA overlap. It keeps V2's three checkpoints,
four interfaces, five effective paired seeds, renderer, sampling contract,
outcomes, and finite-grid analysis. This is a non-confirmatory robustness
extension within the same project, model lineage, renderer, and environment.

## Registered result

All 240 checkpoint--condition--state groups contain five distinct normalized
response payloads; 103/240 vary in the primary content-only recoverable-call
outcome. Native schema exposure increased that outcome at every checkpoint:

| Checkpoint | V3 native-schema contrast |
|---|---:|
| Base | +13.0 percentage points |
| Round 2 | +5.5 percentage points |
| Round 3 | +9.0 percentage points |

The inherited criterion required all three contrasts to be strictly positive,
so it passed. The generated cell and contrast table is
[`paper-table-public.md`](paper-table-public.md).

## Post-hoc route-validity diagnostic

After the registered result was sealed, the same schema-validity decomposition
used for V2 was applied to both response routes. Schema-valid candidates through
either route increased from 101 to 149, 91 to 141, and 89 to 152 per 200
requests at Base, round 2, and round 3: +24.0, +25.0, and +31.5 percentage
points. All 15 checkpoint-by-sample-index contrasts were positive. A strict
replay preserved 495 structured rows, promoted 352 recoverable-text rows,
quarantined 41, and left 312 rows without a candidate. These are post-hoc
candidate-validity counts, not evidence that the actions were appropriate or
useful.

## Trust roots and verification

- Public artifact index SHA-256:
  `9d8f141e027ef3e048b80c795e8a39296e5d0bd9442aaaf25ead19c0aa67ef97`
- Public artifact tree SHA-256:
  `780220e8f13344ba6f44ffcae3d19e9a61a7950f3cbfcc1f2db544826de36c7a`
- Independent local result receipt SHA-256:
  `d3cb265db150c17548773985f6669c8ae1b4bc2a8f20b151884eb8afad8fcdc4`

From a clean checkout, audit the checked-in raw artifact with:

```bash
python3 scripts/opd/audit_trigger_incidence_artifact_v3.py \
  research/artifacts/local-trigger-incidence-v3
```

The audit verifies the closed inventory, runtime and two-stage freeze bindings,
complete 1,200-request grid, seed gates, V2 panel separation, raw classifications,
contrasts, heterogeneity, and claim boundary without contacting a model.

## Claim boundary

V3 supports a checkpoint-conditional same-direction result on a second finite
retained panel. It does not establish independent replication, broad
generalization, checkpoint or training superiority, recovery benefit, action
appropriateness, execution utility, state change, or quest improvement.
